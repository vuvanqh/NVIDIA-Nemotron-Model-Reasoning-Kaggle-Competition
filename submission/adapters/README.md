# Adapter Outputs

Place future LoRA adapter artifacts here after training.

Expected adapter marker files may include `adapter_config.json`, `adapter_model.safetensors`, or `adapter_model.bin`. Do not place the full Nemotron base model in this directory.
