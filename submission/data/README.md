# Data Staging

Use this directory only for lightweight submission-side data references or small samples needed by future scripts.

The prepared training and validation JSONL files currently live outside this folder under `../data_prep/` and are treated as read-only context. Do not commit large datasets here.
